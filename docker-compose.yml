services:
  kiosk-tracker:
    build: .
    container_name: kiosk-tracker
    restart: unless-stopped
    ports:
      - "9005:9005"
    volumes:
      - ./data:/data
    environment:
      - APP_TZ=Europe/Berlin
      - DATA_DIR=/data
